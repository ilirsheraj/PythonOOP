Descriptors control the semantics of attribute access to Python.
They give us a way to intercept and customize the basic attribute operations: get, set, delete at a very low level which in turn unlocks powerful techniques for drastically improving the code reusability
Descriptors are also the internal machinery responsible for a lot of class-related functionality in python, like properties, class methods, slots and more
