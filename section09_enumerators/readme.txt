Enumerations are treated as data types, and you can use them to create sets of constants for use with variables and properties

They were added to Python 3.4 version and are defined as classes that inherit from enum class, which in turn is imported from enum module
from enum import Enum
